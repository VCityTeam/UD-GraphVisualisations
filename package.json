{
  "name": "ud-graphvisualisations",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run dev && npm run serve",
    "start3": "npm run dev && npm run serve3",
    "serve": "python -m http.server",
    "serve3": "python3 -m http.server",
    "build": "webpack --config webpack.config.js",
    "dev": "nodemon --watch src -e js --delay 2500ms --exec npm run build",
    "clean": "rm -rf ./dist && rm -rf ./node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.0",
    "webpack": "^5.90.1",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "buffer": "^6.0.3",
    "d3": "^7.8.5"
  }
}
